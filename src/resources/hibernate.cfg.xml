<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://192.168.1.201:5432/pos_db</property>
        <property name="hibernate.connection.username">data_admin</property>
        <property name="hibernate.connection.password">31427182</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQL9Dialect</property>

        <!-- <property name="hibernate.current_session_context_class">thread</property>
        <property name="hibernate.connection.CharSet">utf8</property>
        <property name="hibernate.connection.characterEncoding">utf8</property>
        <property name="hibernate.connection.useUnicode">true</property> -->

        <!-- HBM2DDL Settings -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapping Files or Annotated Classes-->
        <mapping class="com.pos.Data.Entities.User"/>
        <mapping class="com.pos.Data.Entities.Pos"/>
        <mapping class="com.pos.Data.Entities.Check"/>
        <mapping class="com.pos.Data.Entities.Goods"/>

    </session-factory>
</hibernate-configuration>

